spring:
  application:
    name: review-service
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/reviewsdb
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: bancodedados100%daaula
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      format_sql: true

server:
  port: 7003
  error:
    include-message: always

eureka:
  client:
    service-url:
      defaultZone: